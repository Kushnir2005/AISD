array = [5, 3, 8, 2, 9, 1]

if not array:
    print("Tablica jest pusta.")
else:
    min_value = array[0]
    
    for i in range(1, len(array)):
        if array[i] < min_value:
            min_value = array[i]
    
    print(f"Minimalna wartość w tablicy: {min_value}")
