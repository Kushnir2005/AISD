vector = [7, 3, 9, 2, 5, 1, 8, 6]
max_element = vector[0] 

for i in range(1, len(vector)):
    if vector[i] > max_element:
        max_element = vector[i]

print(f"Największy element wektora: {max_element}")

